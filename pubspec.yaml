name: auh_heung
description: Project AuhHeung - 야생 부족 맹수 정복 RPG
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flame: 1.18.0   # 정확히 고정: 1.23은 Flutter 3.27+ Color API(withValues) 필요 → 3.24.5와 비호환. 1.18.0은 withOpacity 사용(호환)

flutter:
  uses-material-design: true
