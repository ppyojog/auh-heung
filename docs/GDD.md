# 어흥 : 야수의 생존 — Game Design Document

> **AuhHeung : Beast Survivor** · 뱀파이어 서바이버즈류(bullet heaven)
>
> **📚 문서 역할 분담**
> - **이 GDD(.md) = '역사·설계 느낌'** — 정체성·시스템 개요·왜·변천사 (서술형, GitHub서 바로 읽힘)
> - **`AuhHeung_DB.xlsx` = '수치 느낌'** — 무기/적/밸런스 숫자 (엑셀, 보기·편집용 / 정본 [`build_db.py`](build_db.py))
>
> 버전 관리는 git 히스토리(파일명에 버전 숫자 X). 코드: [`../lib/main.dart`](../lib/main.dart).

- **그래픽·사운드: 에셋 0** — 전부 코드 생성(CustomPainter 네온 / Web Audio 없이 PCM 합성).
- **빌드:** Flutter 3.24.5 · 단일 파일 `lib/main.dart` · `--web-renderer html`
  · 투명도는 `color.withOpacity()`만(`withValues` 금지 = 컴파일 실패).
- **라이브:** https://ppyojog.github.io/auh-heung/

---

## 1. 정체성 · USP
한 줄: **"약하고 쫓기던 존재가, 모두가 두려워하는 최강 포식자가 되는 권력 판타지."**

- 뱀서라이크의 *약→신* 성장 곡선이 이 변신 환상을 그대로 구현. 시작은 작고 포위당한
  느낌 → 점점 화면을 짓밟는 압도적 포식자.
- **차별점(클론에 없는 것):**
  1. 게임 이름값 하는 시그니처 **'어흥!' 궁극기**.
  2. 플레이어를 과장되게 떠받드는 **충신(忠臣) herald** — 성장할수록 칭송이 격해짐(무음 텍스트).
  3. 명작들의 장점 + **인디 정체성(영혼)**.
- **펫 타이니:** 작은 타이니가 이동 방향 뒤를 360° 졸졸 따라다니며 표정 반응(평소 🐯 / 처치·레벨업 😻 /
  피격 🙀) + **반투명 말풍선**으로 짧게 말함. 펫 감성으로 무해·귀여움·정서적 애착 강화.
- **타이니 메뉴(허브):** 스테이지 ±선택 · **내 빌드(스킬/패시브/레벨/체력) 상태** · 상점/업적 진입 · 포기 · (치트).
- **추가 컨셉 = 무해함·귀여움(코지 트렌드):** 강함(뽕)과 귀여움 공존. 코드 절차 애니메이션으로
  캐릭터가 숨쉬고·통통 튀고·눈 깜빡이고·꼬리 살랑(정적이지 않음). 캐릭터별 외형(백호 둥근귀/
  흑표 뾰족귀·점/무쇠뿔 뿔). 타이니는 귀엽고 무해하며 떠받드는 마스코트(등장 팝·통통).
- **수익화(욕 안 먹는 법):** 귀여운 **코스메틱 스킨** 중심(P2W·다크패턴 금지). 즉시 재시작 유지.
- **work-discreet:** 충신·연출이 전부 텍스트라 **무음으로도 뽕**. 몰래 하기 좋음.
- **사업:** 웹 자체 수익화는 약함 → 1차 '붙잡고 노는 무료 웹게임' → 모바일 포팅(광고/IAP).

## 2. 코어 루프
1. 타이틀에서 맹수(캐릭터) 선택 → **어흥! 생존 시작**
2. 고정 아레나. **드래그로 이동, 공격은 전자동**
3. 사방에서 의회 졸개가 몰려옴 → 자동 무기로 처치
4. 처치 → 마나 구슬 → 수집 → XP
5. 레벨업 → 일시정지 → **강화 3~4택**(신무기 / 강화 / 진화 / 패시브)
6. 처치로 **광기 게이지** 충전 → 가득 차면 **[어흥!]** 으로 화면 청소 + 광폭화
7. 시간 경과로 적 ↑ → HP 0이면 사망 → 기록 → 재도전 (엔드리스)

## 3. 조작 · UI
- 이동: 화면 어디든 드래그. **방향키는 하단 우측 고정·반투명**(오른손잡이), 노브가 진행 방향 반영.
- 공격: 전자동. 궁극기 **[어흥]** = 하단 좌측 원형 버튼 — **충전 전 반투명, 가득 차면 불투명+발광**.
- **타이니 호출 버튼**(🐯, 어흥 버튼 바로 위, 평소 반투명) → 일시정지 메뉴: 스테이지/적강함×/생존 정보 +
  전리품 상점 + 포기. 게임 내 기능 허브. (음소거 버튼 없음 — 기기 볼륨으로 조절)
- 난이도 = **STAGE**: 플레이 중 45초마다 자동 +1(계속 증가·escalation), 색으로 위협도.
  타이니 메뉴에서 **STAGE ±선택 가능(단, 도달한 최고 스테이지까지만 — 클리어한 곳만)**.
  stage가 높을수록 적 강함(diff=0.85+(stage-1)×0.13) + XP·송곳니·광기 획득도 비례↑(빠른 성장).
  최고 스테이지(maxStage)는 슬롯별 영구 저장.
- 타이니 말풍선은 **반투명**(뒤 적 안 가림) + **표정(😺😼🔥😿🌟)으로 톤 전달**(안 읽어도 인식).
- 카메라 고정. 플레이어는 화면 안으로 클램프.

## 4. 시스템 요약 (수치는 `AuhHeung_DB.xlsx`)
- **캐릭터 3종**: 백호(균형)/흑표(유리대포)/무쇠뿔(탱크) — 시작 무기·스탯 상이.
- **무기 5종**: 발톱·송곳니·포효·벼락·가시밭. 각 레벨 스케일.
- **무기 진화(핵심 훅)**: 최대 레벨 + 시너지 패시브 → 초월 무기(천 개의 발톱/죽음의 고리/대지진).
  레벨업 시 조건 충족하면 진화 선택지 우선 노출.
- **패시브 5종**: 야성(공격)·가죽(체력)·바람(이동)·굶주림(수집)·분노(공속).
- **어흥! 궁극(광기)**: 처치로 충전 → 전 적 피해 + 넉백 + 5초 광폭화(공속·공격·이동 ↑).
- **충신 herald(타이니)**: 시작/레벨업/보스/궁극/위기/학살/마일스톤마다 떠받드는 텍스트 말풍선(무음).
- **적 4종**: grunt/fast/tank/boss. 시간(t)에 비례해 강해지고 종류 추가. 보스 90초 주기.
- **메타 진행(영구강화)**: 사망 시 플레이한 만큼 **송곳니(화폐)** 적립 → 타이틀 **전리품 상점**에서
  영구 강화 구매(공격/체력/이동/수집/송곳니획득). "죽음은 헛되지 않게" 무한 반복의 동력.
- **위협도 표시**: 현재 동적 난이도(`diff`)를 HUD에 등급(안온/평이/거셈/흉포/지옥)+색으로 표시.
- **포식(Devour)+성장**: 처치=삼킴 → 회복 + 호랑이가 시각적으로 커짐(0.9→1.6) = 양→호랑이.
- **업적 8종**: 달성 시 송곳니 보상 + 슬롯별 영구 기록(🏆 화면, 사망 시 새 달성 표시).
- **코스메틱 스킨 6종**: 송곳니로 해금·착용(색만, P2W 없음 = 윤리적 수익화). 🎀 화면.
- **데일리 보너스**: 하루 첫 접속 송곳니 +30(비강제 리텐션).
- **세이브 슬롯 3개**: 슬롯별 송곳니·강화·업적·스킨·기록 분리 저장(`surv_meta_N`/`surv_rec_N`,
  활성 `surv_slot`). 타이틀에서 슬롯 선택(요약 표시).
- **레벨업 연출**: 레벨업 화면에 타이니가 떠받드는 표정+대사(뽕).
- **[치트]** 내부 테스트용 — 타이니 메뉴에 "+10 레벨"(자동 강화 10회). 출시 전 제거 예정.

## 5. 연출 · 게임필 (전부 코드)
- **아트:** 네온 벡터. 어두운 배경 + 발광 레이어. 플레이어=금빛 밝게 / 적=어두운 코어+네온 림으로
  대비 → 잡몹 더미 속 가독성 확보(클론 실패 1순위 원인 차단).
- **손맛:** 데미지 숫자, 화면 흔들림, 발광 파티클·잔상, 펄스 링, 피격 섬광, 보스 체력 링.
- **사운드:** Dart PCM 합성 → WAV → AudioElement. 효과음 7종 + 음소거.

## 6. 기술 · 배포
- Ticker(`createTicker`) dt 루프 + setState 리페인트. `CustomPaint`는 `RepaintBoundary`.
- 배포: `git push` → GitHub Actions(`subosito/flutter-action`) 빌드 → Pages.
  저장소 `github.com/ppyojog/auh-heung`. 빌드 실패해도 직전 라이브 유지.
- **검증:** 로컬 Flutter 미설치 → 수동(괄호·withValues 0·문법) 후 Actions 빌드로 확정.
- 향후: CanvasKit 전환(스웜 300+·maskFilter 글로우; 이모지 코드 교체 필요).

## 7. 코드 구조 (`lib/main.dart`)
- `P`(팔레트), `EType`, 엔티티(`Enemy/Bullet/Orb/Particle/Pulse/FloatText/LineFx`)
- `Tiny`(충신 대사 풀), `Character`/`kChars`, `Upgrade`, `Sfx`(합성 사운드)
- `World`(상태 + `update(dt)` 루프; `_fireWeapons`/`_collide`/`_openLevelUp`/`unleashRoar`/`_say`)
- `GameScreen`(StatefulWidget) + `WorldPainter`(렌더 전담)
- UI: `_hud`/`_rageButton`/`_heraldBubble`/`_title`(캐릭터 선택)/`_levelUp`/`_death`

## 8. 로드맵
**완료:** 네온 아트+손맛 · 합성 사운드 · 무기 진화+신무기(벼락·가시밭) · 캐릭터 3종 ·
초반 난이도 완화 · 조작 개선+어흥! 궁극+스토리 · 충신 herald · 타이니 동적 난이도 선택 ·
메타 영구강화(송곳니→전리품 상점)+위협도 표시

**다음 후보:** 포식(Devour: 삼켜 회복+시각 성장) · 무기/캐릭터 해금(송곳니) ·
보스 패턴/서사 · CanvasKit(스웜 300+) · 엔드리스/데일리/리더보드 · 밸런스 튜닝

## 9. 변경 이력
- (legacy) 투자 은유 카드게임 → 폐기. 로컬 `GDD_..._v28.0.txt` 아카이브로만 보존.
- 피벗1: 텍스트 선택지 → Reigns식 스와이프 카드.
- 피벗2: **뱀파이어 서바이버즈류(현재)**.
- 이후 변경은 git 커밋 로그로 추적.
